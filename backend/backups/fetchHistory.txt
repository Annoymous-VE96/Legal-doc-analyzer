export const fetchHistory = async () => {
  const res = await fetch(`${BASE_URL}/history`, {
    headers: authHeaders(),
  });
  if (!res.ok) throw await res.json();
  return res.json(); // returns array of chats
};